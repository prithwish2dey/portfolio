# SMTP (Nodemailer) settings — preferred for direct SMTP sending
# Example for Gmail SMTP using an app password (if allowed by your provider)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false # true for 465
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password
# Optional: from/to addresses
FROM_EMAIL=no-reply@yourdomain.com
TO_EMAIL=prithwish2dey@gmail.com

# Alternatively, SendGrid (fallback)
# SENDGRID_API_KEY=SG.xxxxx
